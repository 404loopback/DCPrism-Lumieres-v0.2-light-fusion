<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('festival_parameters', function (Blueprint $table) {
            $table->id();
            $table->unsignedBigInteger('festival_id');
            $table->unsignedBigInteger('parameter_id');
            $table->boolean('is_enabled')->default(true)
                ->comment('Le paramètre est-il activé pour ce festival');
            $table->text('custom_default_value')->nullable()
                ->comment('Valeur par défaut personnalisée pour ce festival');
            $table->json('custom_formatting_rules')->nullable()
                ->comment('Règles de formatage spécifiques au festival');
            $table->integer('display_order')->nullable()
                ->comment('Ordre d\'affichage spécifique au festival');
            $table->text('festival_specific_notes')->nullable()
                ->comment('Notes spécifiques au festival pour ce paramètre');
            $table->timestamps();

            // Clés étrangères
            $table->foreign('festival_id')->references('id')->on('festivals')->onDelete('cascade');
            $table->foreign('parameter_id')->references('id')->on('parameters')->onDelete('cascade');

            // Contrainte unique pour éviter les doublons
            $table->unique(['festival_id', 'parameter_id'], 'festival_parameters_unique');

            // Index pour les performances
            $table->index('festival_id', 'festival_parameters_festival_index');
            $table->index('parameter_id', 'festival_parameters_parameter_index');
            $table->index(['festival_id', 'is_enabled'], 'festival_parameters_festival_enabled_index');
            $table->index(['festival_id', 'display_order'], 'festival_parameters_festival_order_index');
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('festival_parameters');
    }
};
