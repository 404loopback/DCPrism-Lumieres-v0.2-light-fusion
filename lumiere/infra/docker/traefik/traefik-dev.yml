api:
  dashboard: true
  insecure: true  # Dashboard accessible sans auth en dev

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false

log:
  level: DEBUG
